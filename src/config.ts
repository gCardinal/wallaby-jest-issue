/**
 * @todo It would be nice if this guy was generated from existing .env files.
 */
export interface Config {
  APP_PORT: string;
  BLENDER_URI: string;
  LOG_LEVEL: string;
  RABBITMQ_USER: string;
  RABBITMQ_PASSWORD: string;
  RABBITMQ_HOST: string;
  RABBITMQ_QUEUE_NAME: string;
}
